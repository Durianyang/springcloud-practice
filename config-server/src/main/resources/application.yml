spring:
  application:
    name: config-server
  cloud:
    config:
      # label 分支
      label: master
      server:
        git:
          # uri git地址
          uri: https://github.com/Durianyang/springcloud-practice
          # searchPaths 表示目录
          searchPaths: repos
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/